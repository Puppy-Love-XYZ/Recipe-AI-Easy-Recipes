# ===================== 核心 AI 框架（LangChain/LangGraph） =====================
# 版本严格对齐，避免子包冲突
langgraph==0.2.10   10 langgraph = 0。2。
langchain==0.2.10
langchain-openai==0.1.7
langchain-core==0.2.22
langchain-community==0.2.10
langsmith==0.1.10

# ===================== 基础数据/类型校验 =====================
pydantic==2.5.2
pydantic-core==2.14.6
pandas==2.1.0   熊猫= =魅惑
numpy==1.26.0

# ===================== 第三方服务集成 =====================
tavily-python==0.3.0
openai==1.30.0

# ===================== Streamlit 网页运行核心 =====================
streamlit==1.35.0
streamlit-chat==0.1.1
watchdog==2.3.0  # Streamlit 热重载必需，部署必加

# ===================== 基础工具依赖（AI 框架必需） =====================
requests==2.31.0   请求= = 2.31.0
tenacity==8.2.3   韧性= = 8.2.3
tiktoken==0.7.0
SQLAlchemy==2.0.29
PyYAML==6.0.1   PyYAML = = 6.0.1中
python-dateutil==2.9.0.post0
typing_extensions==4.11.0

# ===================== 部署优化（避免 pip 干扰） =====================
--disable-pip-version-check——disable-pip-version-check
--no-cache-dir   1 zipp> = 3。18。——no-cache-dir 1 zip > = 3。18。
